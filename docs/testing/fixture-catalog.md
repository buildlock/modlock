# Fixture catalog and rights manifest

> Deferred API or real-corpus design. Current synthetic conformance is governed
> by [contracts/v1](../../contracts/README.md); archived outline schemas are
> not a second active format.


Status: specification complete; real licensed corpus requires creator permission  
Last reviewed: 2026-09-01

Every binary fixture has a sidecar conforming to `docs/design/2026-09-01-contract-outline/schemas/fixture-manifest.schema.json`. Synthetic fixtures are generated from non-copyrightable test bytes/resources; licensed real-world fixtures require documented permission and may be stored in a separate access-controlled repository.

## Synthetic valid fixtures

The [Steam discovery tests](../../crates/modlock-core/src/steam/tests.rs) now
materialize FIX-008-like VDF/ACF inputs and inert exact marker files inside owned
temporary folders, including multiple Unicode-named libraries and hostile paths.
The [discovery guide](steam-discovery-proof.md) distinguishes these authored
oracles from unverified real Steam/current-Deadlock compatibility. No licensed
game/mod bytes are present, and this does not close the full SHO-124 corpus.

The current [Rust journal proof](synthetic-journal-proof.md) generates only inert
ASCII `.txt` files directly from test source, with original/desired byte oracles
and a sibling unowned sentinel. These are authored synthetic bytes, not a mod
binary corpus. The VPK/archive/game fixtures listed below remain planned; the
new text proof does not populate or complete them.

| ID | Input | Expected |
|---|---|---|
| FIX-001 | One minimal raw `_dir.vpk` | Accept; one package inventory |
| FIX-002 | Directory plus numbered split VPK chunks | Accept; complete relationship |
| FIX-003 | ZIP containing one VPK under a benign folder | Accept; normalized inventory |
| FIX-004 | Multiple variants with explicit metadata | Needs variant selection |
| FIX-005 | Two mods with one duplicate internal VPK path | Accept separately; conflict when co-enabled |
| FIX-006 | Unicode filenames and long valid Windows paths | Accept within limits |
| FIX-007 | Existing CFG with comments/mixed line endings | Owned-block round trip preserves unrelated bytes semantically |
| FIX-008 | Valid current-like `libraryfolders.vdf` and app manifest | Discover app 1422450 fixture tree |

## Adversarial/malformed fixtures

| ID | Input | Expected code |
|---|---|---|
| FIX-101 | `../` ZIP traversal | `ARCHIVE_PATH_TRAVERSAL` |
| FIX-102 | Absolute/drive/UNC/device/ADS path | `ARCHIVE_UNSAFE_PATH` |
| FIX-103 | Symlink/hardlink/junction/reparse entry | `ARCHIVE_LINK_REJECTED` |
| FIX-104 | Excessive ratio/expanded bytes/entry count/depth | `ARCHIVE_LIMIT_EXCEEDED` |
| FIX-105 | Encrypted/password archive | `ARCHIVE_ENCRYPTED_UNSUPPORTED` |
| FIX-106 | Nested archive beyond policy | `ARCHIVE_NESTING_EXCEEDED` |
| FIX-107 | Truncated/overlapping VPK ranges | `VPK_MALFORMED` |
| FIX-108 | Missing split VPK part | `VPK_PART_MISSING` |
| FIX-109 | Executable/DLL/driver/script payload | `CONTENT_EXECUTABLE_REJECTED` |
| FIX-110 | Duplicate/corrupt CFG ownership markers | `CFG_OWNERSHIP_CORRUPT` |
| FIX-111 | Replayed/expired/wrong-signature install plan | `PLAN_SIGNATURE_INVALID` or `PLAN_EXPIRED` |
| FIX-112 | Hash/size mismatch after download | `DOWNLOAD_INTEGRITY_FAILED` |

## Failure-injection fixtures

- Process termination before/after every journal transition.
- Disk full during download, extraction, materialization, CFG write, and swap.
- Antivirus removes/quarantines a staged object.
- Destination file locked or concurrently modified.
- Game begins running after planning but before activation.
- `gameinfo.gi` replaced with a compatible new version, unknown structure, or duplicated marker.
- Steam library moves between discovery and activation.
- Network stops/resumes and server ignores/changes range behavior.
- External file is removed or changes bytes between resolution and download.

## Human dependency

The synthetic/adversarial corpus can be generated by Codex during implementation. Ahad must obtain redistribution/internal-test permission for representative real mods and designate a sacrificial Deadlock installation before real-world integration tests.
